{
  "name": "manik-weather-journal-app",
  "version": "1.0.0",
  "description": "This web app queries a weather API by passing a ZIP code entered by the user. From the data returned by the website, temperature and other values are posted as an object on server side. That endpoint object is then fetched by client and UI is changed dynamically.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "manik rana",
  "license": "ISC"
}
